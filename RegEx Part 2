function userName(str){
  var name = /[a-z]_[0-9]{2}$/g;
  var nametest = name.test(str);
  return nametest;
}
console.log(userName("george_98"))

function searchPrice(str){
  var price = /[$]\d{1,3}/;
  var string = price.exec(str);
  return string[0];
}
